
@model List<AGONFRONT.Models.Categoria>

@foreach (var categoria in Model)
{
    <li>
        <a class="dropdown-item" href="@Url.Action("FiltrarPorCategoria", "Productos", new { id = categoria.Id })">
            <i class="bi bi-tag me-2"></i> @categoria.Nombre
        </a>
    </li>
}
